export default async function formatPrintData(ans) {
  console.log('ans.....');
  console.log(JSON.stringify(ans));
  // format print data here
  // send off to print service -- make sure to use awaits and to handle failures properly
}
